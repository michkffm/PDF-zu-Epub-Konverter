[project]
name = "pdf-epub-converter"
version = "0.1.0"
description = "Professional PDF to EPUB converter with OCR support"
authors = [
    {name = "michkffm", email = "michael.koch@vodafone.de"}
]
requires-python = ">=3.9"
dependencies = [
    "pdfplumber>=0.10.0",
    "PyPDF2>=3.0.0",
    "pytesseract>=0.3.10",
    "Pillow>=10.0.0",
    "ebooklib>=0.18.0",
    "lxml>=4.9.0",
    "PyQt6>=6.6.0",
    "python-dotenv>=1.0.0",
]

[tool.poetry]
packages = [
    { include = "pdf_epub_converter", from = "src" }
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "flake8>=6.0.0",
    "mypy>=1.0.0",
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
